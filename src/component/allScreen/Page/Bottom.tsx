import type { JSX } from "react";
import { SmallBottom } from "../../smallScreen/Bottom/SmallBottom";

export function Bottom() : JSX.Element {
            if (window.innerWidth > 1920) {
        
            }
        
            if (window.innerWidth < 800) {
        
            }
            
            if (window.innerWidth < 1280) {
        
            }
        
            return <SmallBottom />
}